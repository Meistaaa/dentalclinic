NODE_ENV=development
PORT=3000
DATABASE_URL=postgresql://YOUR_USER:YOUR_PASSWORD@localhost:5432/dentalclinic
# Comma-separated browser origins allowed to call this API (no wildcards: CORS runs with credentials).
CORS_ORIGIN=http://localhost:5173
